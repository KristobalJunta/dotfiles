# ~/.gitconfig
# by @KristobalJunta

[push]
	default = simple
[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	t = tag
	unstage = reset HEAD --
	all = add .
	l = log --oneline
[include]
    path = ~/.gitconfig.user
[core]
	autocrlf = input
